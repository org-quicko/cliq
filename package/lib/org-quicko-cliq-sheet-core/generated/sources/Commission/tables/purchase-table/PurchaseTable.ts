// Auto-generated by @org.quicko/sheet-generator@6.0.2

import { JSONArray } from '@org.quicko/core';
import { Table } from '@org.quicko/sheet-core';
import { PurchaseRow } from './PurchaseRow';

@Reflect.metadata('name', 'purchase_table')
export class PurchaseTable extends Table {
    private static header = new JSONArray(
        Array.from([
            'purchase_id',
			'purchase_date',
			'contact_id',
			'item_id',
			'amount',
			'commission',
			'external_id',
			'utm_id',
			'utm_source',
			'utm_medium',
			'utm_campaign',
			'utm_term',
			'utm_content'
        ])
    );

    constructor() {
        super();
        super.setHeader(PurchaseTable.header);
    }

    override getRow(index: number): PurchaseRow {
        const row = super.getRow(index);

        if (row instanceof PurchaseRow) {
            return row;
        }

        const tableRow = new PurchaseRow(row);
        this.replaceRow(index, tableRow);
        return tableRow;
    }

    override addRow(row: PurchaseRow): void {
        super.addRow(row);
    }

    override replaceRow(index: number, row: PurchaseRow): void {
        super.replaceRow(index, row);
    }
}