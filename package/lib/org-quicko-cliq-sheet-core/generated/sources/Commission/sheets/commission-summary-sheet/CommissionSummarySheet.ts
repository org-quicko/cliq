// Auto-generated by @org.quicko/sheet-generator@6.0.2

import { Sheet, Block } from '@org.quicko/sheet-core';
import { Expose, Type } from 'class-transformer';
import { ValidateNested } from 'class-validator';
import 'reflect-metadata';
import { CommissionSummaryList } from '../../lists/commission-summary-list/CommissionSummaryList'

@Reflect.metadata('name', 'commission_summary_sheet')
export class CommissionSummarySheet extends Sheet {
    @Expose()
    @ValidateNested({ each: true })
    @Type(() => Block, {
        keepDiscriminatorProperty: true,
        discriminator: {
            property: 'name',
            subTypes: [
                {
					value: CommissionSummaryList,
					name: 'commission_summary_list'
				}
            ],
        },
    })
    blocks: Array<Block> = new Array<Block>();

    
    addCommissionSummaryList(list: CommissionSummaryList): void {
        this.addBlock(list);
    }

    getCommissionSummaryList(): CommissionSummaryList {
        if (!this.getBlock('commission_summary_list')) {
            this.addBlock(new CommissionSummaryList());
        }
        return this.getBlock('commission_summary_list') as CommissionSummaryList;
    }
}