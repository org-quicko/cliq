// Auto-generated by @org.quicko/sheet-generator@6.0.2

import { Sheet, Block } from '@org.quicko/sheet-core';
import { Expose, Type } from 'class-transformer';
import { ValidateNested } from 'class-validator';
import 'reflect-metadata';
import { SignupTable } from '../../tables/signup-table/SignupTable'

@Reflect.metadata('name', 'signup_sheet')
export class SignupSheet extends Sheet {
    @Expose()
    @ValidateNested({ each: true })
    @Type(() => Block, {
        keepDiscriminatorProperty: true,
        discriminator: {
            property: 'name',
            subTypes: [
                {
					value: SignupTable,
					name: 'signup_table'
				}
            ],
        },
    })
    blocks: Array<Block> = new Array<Block>();

    
    addSignupTable(table: SignupTable): void {
        this.addBlock(table);
    }

    getSignupTable(): SignupTable {
        if (!this.getBlock('signup_table')) {
            this.addBlock(new SignupTable());
        }
        return this.getBlock('signup_table') as SignupTable;
    }
}