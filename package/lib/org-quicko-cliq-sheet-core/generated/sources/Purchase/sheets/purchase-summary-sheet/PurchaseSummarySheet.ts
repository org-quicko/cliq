// Auto-generated by @org.quicko/sheet-generator@6.0.2

import { Sheet, Block } from '@org.quicko/sheet-core';
import { Expose, Type } from 'class-transformer';
import { ValidateNested } from 'class-validator';
import 'reflect-metadata';
import { PurchaseSummaryList } from '../../lists/purchase-summary-list/PurchaseSummaryList'

@Reflect.metadata('name', 'purchase_summary_sheet')
export class PurchaseSummarySheet extends Sheet {
    @Expose()
    @ValidateNested({ each: true })
    @Type(() => Block, {
        keepDiscriminatorProperty: true,
        discriminator: {
            property: 'name',
            subTypes: [
                {
					value: PurchaseSummaryList,
					name: 'purchase_summary_list'
				}
            ],
        },
    })
    blocks: Array<Block> = new Array<Block>();

    
    addPurchaseSummaryList(list: PurchaseSummaryList): void {
        this.addBlock(list);
    }

    getPurchaseSummaryList(): PurchaseSummaryList {
        if (!this.getBlock('purchase_summary_list')) {
            this.addBlock(new PurchaseSummaryList());
        }
        return this.getBlock('purchase_summary_list') as PurchaseSummaryList;
    }
}