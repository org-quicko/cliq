// Auto-generated by @org.quicko/sheet-generator@6.0.2

import { Sheet, Block } from '@org.quicko/sheet-core';
import { Expose, Type } from 'class-transformer';
import { ValidateNested } from 'class-validator';
import 'reflect-metadata';
import { CommissionTable } from '../../tables/commission-table/CommissionTable'

@Reflect.metadata('name', 'commission_sheet')
export class CommissionSheet extends Sheet {
    @Expose()
    @ValidateNested({ each: true })
    @Type(() => Block, {
        keepDiscriminatorProperty: true,
        discriminator: {
            property: 'name',
            subTypes: [
                {
					value: CommissionTable,
					name: 'commission_table'
				}
            ],
        },
    })
    blocks: Array<Block> = new Array<Block>();

    
    addCommissionTable(table: CommissionTable): void {
        this.addBlock(table);
    }

    getCommissionTable(): CommissionTable {
        if (!this.getBlock('commission_table')) {
            this.addBlock(new CommissionTable());
        }
        return this.getBlock('commission_table') as CommissionTable;
    }
}