// Auto-generated by @org.quicko/sheet-generator@6.0.2

import { Sheet, Block } from '@org.quicko/sheet-core';
import { Expose, Type } from 'class-transformer';
import { ValidateNested } from 'class-validator';
import 'reflect-metadata';
import { ReferralTable } from '../../tables/referral-table/ReferralTable'

@Reflect.metadata('name', 'referral_sheet')
export class ReferralSheet extends Sheet {
    @Expose()
    @ValidateNested({ each: true })
    @Type(() => Block, {
        keepDiscriminatorProperty: true,
        discriminator: {
            property: 'name',
            subTypes: [
                {
					value: ReferralTable,
					name: 'referral_table'
				}
            ],
        },
    })
    blocks: Array<Block> = new Array<Block>();

    
    addReferralTable(table: ReferralTable): void {
        this.addBlock(table);
    }

    getReferralTable(): ReferralTable {
        if (!this.getBlock('referral_table')) {
            this.addBlock(new ReferralTable());
        }
        return this.getBlock('referral_table') as ReferralTable;
    }
}