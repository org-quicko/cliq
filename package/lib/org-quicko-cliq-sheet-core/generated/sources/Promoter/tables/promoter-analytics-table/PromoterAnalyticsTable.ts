// Auto-generated by @org.quicko/sheet-generator@6.0.2

import { JSONArray } from '@org.quicko/core';
import { Table } from '@org.quicko/sheet-core';
import { PromoterAnalyticsRow } from './PromoterAnalyticsRow';

@Reflect.metadata('name', 'promoter_analytics_table')
export class PromoterAnalyticsTable extends Table {
    private static header = new JSONArray(
        Array.from([
            'program_id',
			'promoter_id',
			'total_signups',
			'total_purchases',
			'total_revenue',
			'total_commission'
        ])
    );

    constructor() {
        super();
        super.setHeader(PromoterAnalyticsTable.header);
    }

    override getRow(index: number): PromoterAnalyticsRow {
        const row = super.getRow(index);

        if (row instanceof PromoterAnalyticsRow) {
            return row;
        }

        const tableRow = new PromoterAnalyticsRow(row);
        this.replaceRow(index, tableRow);
        return tableRow;
    }

    override addRow(row: PromoterAnalyticsRow): void {
        super.addRow(row);
    }

    override replaceRow(index: number, row: PromoterAnalyticsRow): void {
        super.replaceRow(index, row);
    }
}