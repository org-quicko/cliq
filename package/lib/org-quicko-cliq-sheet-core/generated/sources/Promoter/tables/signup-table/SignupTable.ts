// Auto-generated by @org.quicko/sheet-generator@6.0.2

import { JSONArray } from '@org.quicko/core';
import { Table } from '@org.quicko/sheet-core';
import { SignupRow } from './SignupRow';

@Reflect.metadata('name', 'signup_table')
export class SignupTable extends Table {
    private static header = new JSONArray(
        Array.from([
            'contact_id',
			'first_name',
			'last_name',
			'email',
			'phone',
			'link_id',
			'created_at'
        ])
    );

    constructor() {
        super();
        super.setHeader(SignupTable.header);
    }

    override getRow(index: number): SignupRow {
        const row = super.getRow(index);

        if (row instanceof SignupRow) {
            return row;
        }

        const tableRow = new SignupRow(row);
        this.replaceRow(index, tableRow);
        return tableRow;
    }

    override addRow(row: SignupRow): void {
        super.addRow(row);
    }

    override replaceRow(index: number, row: SignupRow): void {
        super.replaceRow(index, row);
    }
}