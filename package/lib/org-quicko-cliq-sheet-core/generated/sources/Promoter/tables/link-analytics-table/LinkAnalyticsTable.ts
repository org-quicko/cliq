// Auto-generated by @org.quicko/sheet-generator@6.0.2

import { JSONArray } from '@org.quicko/core';
import { Table } from '@org.quicko/sheet-core';
import { LinkAnalyticsRow } from './LinkAnalyticsRow';

@Reflect.metadata('name', 'link_analytics_table')
export class LinkAnalyticsTable extends Table {
    private static header = new JSONArray(
        Array.from([
            'link_id',
			'link_name',
			'ref_val',
			'promoter_id',
			'signups',
			'purchases',
			'commission',
			'created_at'
        ])
    );

    constructor() {
        super();
        super.setHeader(LinkAnalyticsTable.header);
    }

    override getRow(index: number): LinkAnalyticsRow {
        const row = super.getRow(index);

        if (row instanceof LinkAnalyticsRow) {
            return row;
        }

        const tableRow = new LinkAnalyticsRow(row);
        this.replaceRow(index, tableRow);
        return tableRow;
    }

    override addRow(row: LinkAnalyticsRow): void {
        super.addRow(row);
    }

    override replaceRow(index: number, row: LinkAnalyticsRow): void {
        super.replaceRow(index, row);
    }
}