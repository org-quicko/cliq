// Auto-generated by @org.quicko/sheet-generator@6.0.2

import { JSONArray } from '@org.quicko/core';
import { Table } from '@org.quicko/sheet-core';
import { MemberRow } from './MemberRow';

@Reflect.metadata('name', 'member_table')
export class MemberTable extends Table {
    private static header = new JSONArray(
        Array.from([
            'member_id',
			'first_name',
			'last_name',
			'email',
			'role',
			'added_on'
        ])
    );

    constructor() {
        super();
        super.setHeader(MemberTable.header);
    }

    override getRow(index: number): MemberRow {
        const row = super.getRow(index);

        if (row instanceof MemberRow) {
            return row;
        }

        const tableRow = new MemberRow(row);
        this.replaceRow(index, tableRow);
        return tableRow;
    }

    override addRow(row: MemberRow): void {
        super.addRow(row);
    }

    override replaceRow(index: number, row: MemberRow): void {
        super.replaceRow(index, row);
    }
}