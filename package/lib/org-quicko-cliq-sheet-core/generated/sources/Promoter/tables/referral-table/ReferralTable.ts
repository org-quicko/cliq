// Auto-generated by @org.quicko/sheet-generator@6.0.2

import { JSONArray } from '@org.quicko/core';
import { Table } from '@org.quicko/sheet-core';
import { ReferralRow } from './ReferralRow';

@Reflect.metadata('name', 'referral_table')
export class ReferralTable extends Table {
    private static header = new JSONArray(
        Array.from([
            'promoter_id',
			'status',
			'contact_id',
			'contact_info',
			'total_revenue',
			'total_commission',
			'updated_at'
        ])
    );

    constructor() {
        super();
        super.setHeader(ReferralTable.header);
    }

    override getRow(index: number): ReferralRow {
        const row = super.getRow(index);

        if (row instanceof ReferralRow) {
            return row;
        }

        const tableRow = new ReferralRow(row);
        this.replaceRow(index, tableRow);
        return tableRow;
    }

    override addRow(row: ReferralRow): void {
        super.addRow(row);
    }

    override replaceRow(index: number, row: ReferralRow): void {
        super.replaceRow(index, row);
    }
}