// Auto-generated by @org.quicko/sheet-generator@6.0.2

import { Workbook, Sheet } from '@org.quicko/sheet-core';
import { Expose, Type } from 'class-transformer';
import { ValidateNested } from 'class-validator';
import 'reflect-metadata';
import { ProgramSummarySheet } from '../sheets/program-summary-sheet/ProgramSummarySheet';
import { PromoterSheet } from '../sheets/promoter-sheet/PromoterSheet';

@Reflect.metadata('name', 'ProgramWorkbook')
export class ProgramWorkbook extends Workbook {
    @Expose()
    @ValidateNested({ each: true })
    @Type(() => Sheet, {
        keepDiscriminatorProperty: true,
        discriminator: {
            property: 'name',
            subTypes: [
                {
					value: ProgramSummarySheet,
					name: 'program_summary_sheet'
				},
				{
					value: PromoterSheet,
					name: 'promoter_sheet'
				}
            ],
        },
    })
    sheets: Array<Sheet> = new Array<Sheet>();
    
    addProgramSummarySheet(sheet: ProgramSummarySheet): void {
        this.addSheet(sheet);
    }

    getProgramSummarySheet(): ProgramSummarySheet {
        if (!this.getSheet('program_summary_sheet')) {
            this.addSheet(new ProgramSummarySheet());
        }
        return this.getSheet('program_summary_sheet') as ProgramSummarySheet;
    }

    addPromoterSheet(sheet: PromoterSheet): void {
        this.addSheet(sheet);
    }

    getPromoterSheet(): PromoterSheet {
        if (!this.getSheet('promoter_sheet')) {
            this.addSheet(new PromoterSheet());
        }
        return this.getSheet('promoter_sheet') as PromoterSheet;
    }
}