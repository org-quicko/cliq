// Auto-generated by @org.quicko/sheet-generator@6.0.2

import { Sheet, Block } from '@org.quicko/sheet-core';
import { Expose, Type } from 'class-transformer';
import { ValidateNested } from 'class-validator';
import 'reflect-metadata';
import { PromoterTable } from '../../tables/promoter-table/PromoterTable'

@Reflect.metadata('name', 'promoter_sheet')
export class PromoterSheet extends Sheet {
    @Expose()
    @ValidateNested({ each: true })
    @Type(() => Block, {
        keepDiscriminatorProperty: true,
        discriminator: {
            property: 'name',
            subTypes: [
                {
					value: PromoterTable,
					name: 'promoter_table'
				}
            ],
        },
    })
    blocks: Array<Block> = new Array<Block>();

    
    addPromoterTable(table: PromoterTable): void {
        this.addBlock(table);
    }

    getPromoterTable(): PromoterTable {
        if (!this.getBlock('promoter_table')) {
            this.addBlock(new PromoterTable());
        }
        return this.getBlock('promoter_table') as PromoterTable;
    }
}