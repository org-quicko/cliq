// Auto-generated by @org.quicko/sheet-generator@6.0.2

import { JSONArray } from '@org.quicko/core';
import { Table } from '@org.quicko/sheet-core';
import { PromoterRow } from './PromoterRow';

@Reflect.metadata('name', 'promoter_table')
export class PromoterTable extends Table {
    private static header = new JSONArray(
        Array.from([
            'promoter_id',
			'promoter_name',
			'status',
			'signups',
			'commission_on_signups',
			'purchases',
			'revenue',
			'commission_on_purchases',
			'total_commission'
        ])
    );

    constructor() {
        super();
        super.setHeader(PromoterTable.header);
    }

    override getRow(index: number): PromoterRow {
        const row = super.getRow(index);

        if (row instanceof PromoterRow) {
            return row;
        }

        const tableRow = new PromoterRow(row);
        this.replaceRow(index, tableRow);
        return tableRow;
    }

    override addRow(row: PromoterRow): void {
        super.addRow(row);
    }

    override replaceRow(index: number, row: PromoterRow): void {
        super.replaceRow(index, row);
    }
}