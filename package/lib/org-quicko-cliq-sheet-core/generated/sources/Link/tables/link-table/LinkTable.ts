// Auto-generated by @org.quicko/sheet-generator@6.0.2

import { JSONArray } from '@org.quicko/core';
import { Table } from '@org.quicko/sheet-core';
import { LinkRow } from './LinkRow';

@Reflect.metadata('name', 'link_table')
export class LinkTable extends Table {
    private static header = new JSONArray(
        Array.from([
            'link_name',
			'link',
			'signups',
			'commission_on_signups',
			'purchases',
			'revenue',
			'commission_on_purchases',
			'total_commission'
        ])
    );

    constructor() {
        super();
        super.setHeader(LinkTable.header);
    }

    override getRow(index: number): LinkRow {
        const row = super.getRow(index);

        if (row instanceof LinkRow) {
            return row;
        }

        const tableRow = new LinkRow(row);
        this.replaceRow(index, tableRow);
        return tableRow;
    }

    override addRow(row: LinkRow): void {
        super.addRow(row);
    }

    override replaceRow(index: number, row: LinkRow): void {
        super.replaceRow(index, row);
    }
}