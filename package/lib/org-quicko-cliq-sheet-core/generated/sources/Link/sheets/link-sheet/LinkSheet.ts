// Auto-generated by @org.quicko/sheet-generator@6.0.2

import { Sheet, Block } from '@org.quicko/sheet-core';
import { Expose, Type } from 'class-transformer';
import { ValidateNested } from 'class-validator';
import 'reflect-metadata';
import { LinkTable } from '../../tables/link-table/LinkTable'

@Reflect.metadata('name', 'link_sheet')
export class LinkSheet extends Sheet {
    @Expose()
    @ValidateNested({ each: true })
    @Type(() => Block, {
        keepDiscriminatorProperty: true,
        discriminator: {
            property: 'name',
            subTypes: [
                {
					value: LinkTable,
					name: 'link_table'
				}
            ],
        },
    })
    blocks: Array<Block> = new Array<Block>();

    
    addLinkTable(table: LinkTable): void {
        this.addBlock(table);
    }

    getLinkTable(): LinkTable {
        if (!this.getBlock('link_table')) {
            this.addBlock(new LinkTable());
        }
        return this.getBlock('link_table') as LinkTable;
    }
}