// Auto-generated by @org.quicko/sheet-generator@6.0.2

import { Sheet, Block } from '@org.quicko/sheet-core';
import { Expose, Type } from 'class-transformer';
import { ValidateNested } from 'class-validator';
import 'reflect-metadata';
import { LinkSummaryList } from '../../lists/link-summary-list/LinkSummaryList'

@Reflect.metadata('name', 'link_summary_sheet')
export class LinkSummarySheet extends Sheet {
    @Expose()
    @ValidateNested({ each: true })
    @Type(() => Block, {
        keepDiscriminatorProperty: true,
        discriminator: {
            property: 'name',
            subTypes: [
                {
					value: LinkSummaryList,
					name: 'link_summary_list'
				}
            ],
        },
    })
    blocks: Array<Block> = new Array<Block>();

    
    addLinkSummaryList(list: LinkSummaryList): void {
        this.addBlock(list);
    }

    getLinkSummaryList(): LinkSummaryList {
        if (!this.getBlock('link_summary_list')) {
            this.addBlock(new LinkSummaryList());
        }
        return this.getBlock('link_summary_list') as LinkSummaryList;
    }
}