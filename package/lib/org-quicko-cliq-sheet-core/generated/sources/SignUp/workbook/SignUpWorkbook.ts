// Auto-generated by @org.quicko/sheet-generator@6.0.2

import { Workbook, Sheet } from '@org.quicko/sheet-core';
import { Expose, Type } from 'class-transformer';
import { ValidateNested } from 'class-validator';
import 'reflect-metadata';
import { SignupSummarySheet } from '../sheets/signup-summary-sheet/SignupSummarySheet';
import { SignupSheet } from '../sheets/signup-sheet/SignupSheet';

@Reflect.metadata('name', 'SignUpWorkbook')
export class SignUpWorkbook extends Workbook {
    @Expose()
    @ValidateNested({ each: true })
    @Type(() => Sheet, {
        keepDiscriminatorProperty: true,
        discriminator: {
            property: 'name',
            subTypes: [
                {
					value: SignupSummarySheet,
					name: 'signup_summary_sheet'
				},
				{
					value: SignupSheet,
					name: 'signup_sheet'
				}
            ],
        },
    })
    sheets: Array<Sheet> = new Array<Sheet>();
    
    addSignupSummarySheet(sheet: SignupSummarySheet): void {
        this.addSheet(sheet);
    }

    getSignupSummarySheet(): SignupSummarySheet {
        if (!this.getSheet('signup_summary_sheet')) {
            this.addSheet(new SignupSummarySheet());
        }
        return this.getSheet('signup_summary_sheet') as SignupSummarySheet;
    }

    addSignupSheet(sheet: SignupSheet): void {
        this.addSheet(sheet);
    }

    getSignupSheet(): SignupSheet {
        if (!this.getSheet('signup_sheet')) {
            this.addSheet(new SignupSheet());
        }
        return this.getSheet('signup_sheet') as SignupSheet;
    }
}